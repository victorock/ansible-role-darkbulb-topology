- name: "Darkbulb Topology"
  hosts: darkbulb_topology
  gather_facts: no
  become: yes

  roles:
    - test

  tasks:

    - include_role:
        name: test
        tasks_from: run
      vars:
        darkbulb_topology_state: 'present'
