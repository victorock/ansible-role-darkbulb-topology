- name: "Darkbulb Topology"
  hosts: darkbulb_topology:&nxos
  gather_facts: no
  become: yes

  roles:
    - test

  tasks:
    - include_role:
        name: test
      vars:
        autorun: true
        darkbulb_topology_config:
            state: "present"
