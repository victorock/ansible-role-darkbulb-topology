- hosts: topology
  gather_facts: no
  become: yes
  # Hack to avoid race-condition when running multi-node topology in a single host
  # Avoids race-condition in libvirt when setting up common resources
  serial:
  - 1

  roles:
    - role: role_under_test
      autorun: yes
      topology_builder_state: absent
