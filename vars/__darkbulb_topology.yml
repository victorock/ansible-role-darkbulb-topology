__darkbulb_topology:
  config:       "{{ __config                                                                               }}"
  compute:      "{{ __domain[topology_builder_device_os]                                                   }}"
  network:      "{{ __network[(topology_builder_network['network']['forward']['@mode']|default('nat'))]    }}"
  storagepool:  "{{ __pool[(topology_builder_storagepool['pool']['@type']|default('dir'))]                 }}"
  storagevol:   "{{ __vol[topology_builder_device_os]                                                      }}"
