__domain_os:
  asa:
    boot:
        '@dev':   hd
    type:
        $:        hvm
        '@arch':  x86_64
  eos:
    boot:
        '@dev':   hd
    type:
        $:        hvm
        '@arch':  x86_64
  ios:
    boot:
        '@dev':   hd
    type:
        $:        hvm
        '@arch':  x86_64
  iosxr:
    boot:
        '@dev':   hd
    type:
        $:        hvm
        '@arch':  x86_64
  junos:
    boot:
        '@dev':   hd
    type:
        $:        hvm
        '@arch':  x86_64
  nxos:
    boot:
        '@dev':   hd
    type:
        $:        hvm
        '@arch':  x86_64
  ovs:
    boot:
        '@dev':   hd
    type:
        $:        hvm
        '@arch':  x86_64
  vyos:
    boot:
        '@dev':   hd
    type:
        $:        hvm
        '@arch':  x86_64
