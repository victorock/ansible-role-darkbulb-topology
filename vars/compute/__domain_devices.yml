__domain_devices:
  asa:
    console:    "{{ __domain_device_console[topology_builder_network_os] }}"
    disk:       "{{ __domain_device_disk[topology_builder_network_os] }}"
    interface:  "{{ __domain_device_interface[topology_builder_network_os] }}"
    serial:     "{{ __domain_device_serial[topology_builder_network_os] }}"
    graphics:   "{{ __domain_device_graphics[topology_builder_network_os] }}"
    video:      "{{ __domain_device_video[topology_builder_network_os] }}"
    input:      "{{ __domain_device_input[topology_builder_network_os] }}"
    controller: "{{ __domain_device_controller[topology_builder_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[topology_builder_network_os] }}"
  eos:
    console:    "{{ __domain_device_console[topology_builder_network_os] }}"
    disk:       "{{ __domain_device_disk[topology_builder_network_os] }}"
    interface:  "{{ __domain_device_interface[topology_builder_network_os] }}"
    serial:     "{{ __domain_device_serial[topology_builder_network_os] }}"
    graphics:   "{{ __domain_device_graphics[topology_builder_network_os] }}"
    video:      "{{ __domain_device_video[topology_builder_network_os] }}"
    input:      "{{ __domain_device_input[topology_builder_network_os] }}"
    controller: "{{ __domain_device_controller[topology_builder_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[topology_builder_network_os] }}"
  ios:
    console:    "{{ __domain_device_console[topology_builder_network_os] }}"
    disk:       "{{ __domain_device_disk[topology_builder_network_os] }}"
    interface:  "{{ __domain_device_interface[topology_builder_network_os] }}"
    serial:     "{{ __domain_device_serial[topology_builder_network_os] }}"
    graphics:   "{{ __domain_device_graphics[topology_builder_network_os] }}"
    video:      "{{ __domain_device_video[topology_builder_network_os] }}"
    input:      "{{ __domain_device_input[topology_builder_network_os] }}"
    controller: "{{ __domain_device_controller[topology_builder_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[topology_builder_network_os] }}"
  iosxr:
    console:    "{{ __domain_device_console[topology_builder_network_os] }}"
    disk:       "{{ __domain_device_disk[topology_builder_network_os] }}"
    interface:  "{{ __domain_device_interface[topology_builder_network_os] }}"
    serial:     "{{ __domain_device_serial[topology_builder_network_os] }}"
    graphics:   "{{ __domain_device_graphics[topology_builder_network_os] }}"
    video:      "{{ __domain_device_video[topology_builder_network_os] }}"
    input:      "{{ __domain_device_input[topology_builder_network_os] }}"
    controller: "{{ __domain_device_controller[topology_builder_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[topology_builder_network_os] }}"
  junos:
    console:    "{{ __domain_device_console[topology_builder_network_os] }}"
    disk:       "{{ __domain_device_disk[topology_builder_network_os] }}"
    interface:  "{{ __domain_device_interface[topology_builder_network_os] }}"
    serial:     "{{ __domain_device_serial[topology_builder_network_os] }}"
    graphics:   "{{ __domain_device_graphics[topology_builder_network_os] }}"
    video:      "{{ __domain_device_video[topology_builder_network_os] }}"
    input:      "{{ __domain_device_input[topology_builder_network_os] }}"
    controller: "{{ __domain_device_controller[topology_builder_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[topology_builder_network_os] }}"
  nxos:
    console:    "{{ __domain_device_console[topology_builder_network_os] }}"
    controller: "{{ __domain_device_controller[topology_builder_network_os] }}"
    disk:       "{{ __domain_device_disk[topology_builder_network_os] }}"
    interface:  "{{ __domain_device_interface[topology_builder_network_os] }}"
    serial:     "{{ __domain_device_serial[topology_builder_network_os] }}"
    graphics:   "{{ __domain_device_graphics[topology_builder_network_os] }}"
    video:      "{{ __domain_device_video[topology_builder_network_os] }}"
    input:      "{{ __domain_device_input[topology_builder_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[topology_builder_network_os] }}"
  ovs:
    console:    "{{ __domain_device_console[topology_builder_network_os] }}"
    disk:       "{{ __domain_device_disk[topology_builder_network_os] }}"
    interface:  "{{ __domain_device_interface[topology_builder_network_os] }}"
    serial:     "{{ __domain_device_serial[topology_builder_network_os] }}"
    graphics:   "{{ __domain_device_graphics[topology_builder_network_os] }}"
    video:      "{{ __domain_device_video[topology_builder_network_os] }}"
    input:      "{{ __domain_device_input[topology_builder_network_os] }}"
    controller: "{{ __domain_device_controller[topology_builder_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[topology_builder_network_os] }}"
  vyos:
    console:    "{{ __domain_device_console[topology_builder_network_os] }}"
    disk:       "{{ __domain_device_disk[topology_builder_network_os] }}"
    interface:  "{{ __domain_device_interface[topology_builder_network_os] }}"
    serial:     "{{ __domain_device_serial[topology_builder_network_os] }}"
    graphics:   "{{ __domain_device_graphics[topology_builder_network_os] }}"
    video:      "{{ __domain_device_video[topology_builder_network_os] }}"
    input:      "{{ __domain_device_input[topology_builder_network_os] }}"
    controller: "{{ __domain_device_controller[topology_builder_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[topology_builder_network_os] }}"
