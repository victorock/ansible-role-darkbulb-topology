__domain_devices:
  asa:
    console:    "{{ __domain_device_console[ansible_network_os] }}"
    disk:       "{{ __domain_device_disk[ansible_network_os] }}"
    interface:  "{{ __domain_device_interface[ansible_network_os] }}"
    serial:     "{{ __domain_device_serial[ansible_network_os] }}"
#    graphics:   "{{ __domain_device_graphics[ansible_network_os] }}"
#    video:      "{{ __domain_device_video[ansible_network_os] }}"
#    input:      "{{ __domain_device_input[ansible_network_os] }}"
    controller: "{{ __domain_device_controller[ansible_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[ansible_network_os] }}"
  eos:
    console:    "{{ __domain_device_console[ansible_network_os] }}"
    disk:       "{{ __domain_device_disk[ansible_network_os] }}"
    interface:  "{{ __domain_device_interface[ansible_network_os] }}"
    serial:     "{{ __domain_device_serial[ansible_network_os] }}"
#    graphics:   "{{ __domain_device_graphics[ansible_network_os] }}"
#    video:      "{{ __domain_device_video[ansible_network_os] }}"
#    input:      "{{ __domain_device_input[ansible_network_os] }}"
    controller: "{{ __domain_device_controller[ansible_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[ansible_network_os] }}"
  ios:
    console:    "{{ __domain_device_console[ansible_network_os] }}"
    disk:       "{{ __domain_device_disk[ansible_network_os] }}"
    interface:  "{{ __domain_device_interface[ansible_network_os] }}"
    serial:     "{{ __domain_device_serial[ansible_network_os] }}"
#    graphics:   "{{ __domain_device_graphics[ansible_network_os] }}"
#    video:      "{{ __domain_device_video[ansible_network_os] }}"
#    input:      "{{ __domain_device_input[ansible_network_os] }}"
#    controller: "{{ __domain_device_controller[ansible_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[ansible_network_os] }}"
  iosxr:
    console:    "{{ __domain_device_console[ansible_network_os] }}"
    disk:       "{{ __domain_device_disk[ansible_network_os] }}"
    interface:  "{{ __domain_device_interface[ansible_network_os] }}"
    serial:     "{{ __domain_device_serial[ansible_network_os] }}"
#    graphics:   "{{ __domain_device_graphics[ansible_network_os] }}"
#    video:      "{{ __domain_device_video[ansible_network_os] }}"
#    input:      "{{ __domain_device_input[ansible_network_os] }}"
    controller: "{{ __domain_device_controller[ansible_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[ansible_network_os] }}"
  junos:
    console:    "{{ __domain_device_console[ansible_network_os] }}"
    disk:       "{{ __domain_device_disk[ansible_network_os] }}"
    interface:  "{{ __domain_device_interface[ansible_network_os] }}"
    serial:     "{{ __domain_device_serial[ansible_network_os] }}"
#    graphics:   "{{ __domain_device_graphics[ansible_network_os] }}"
#    video:      "{{ __domain_device_video[ansible_network_os] }}"
#    input:      "{{ __domain_device_input[ansible_network_os] }}"
    controller: "{{ __domain_device_controller[ansible_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[ansible_network_os] }}"
  nxos:
    console:    "{{ __domain_device_console[ansible_network_os] }}"
    disk:       "{{ __domain_device_disk[ansible_network_os] }}"
    interface:  "{{ __domain_device_interface[ansible_network_os] }}"
    serial:     "{{ __domain_device_serial[ansible_network_os] }}"
#    graphics:   "{{ __domain_device_graphics[ansible_network_os] }}"
#    video:      "{{ __domain_device_video[ansible_network_os] }}"
#    input:      "{{ __domain_device_input[ansible_network_os] }}"
    controller: "{{ __domain_device_controller[ansible_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[ansible_network_os] }}"
  ovs:
    console:    "{{ __domain_device_console[ansible_network_os] }}"
    disk:       "{{ __domain_device_disk[ansible_network_os] }}"
    interface:  "{{ __domain_device_interface[ansible_network_os] }}"
    serial:     "{{ __domain_device_serial[ansible_network_os] }}"
    graphics:   "{{ __domain_device_graphics[ansible_network_os] }}"
    video:      "{{ __domain_device_video[ansible_network_os] }}"
    input:      "{{ __domain_device_input[ansible_network_os] }}"
    controller: "{{ __domain_device_controller[ansible_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[ansible_network_os] }}"
  vyos:
    console:    "{{ __domain_device_console[ansible_network_os] }}"
    disk:       "{{ __domain_device_disk[ansible_network_os] }}"
    interface:  "{{ __domain_device_interface[ansible_network_os] }}"
    serial:     "{{ __domain_device_serial[ansible_network_os] }}"
    graphics:   "{{ __domain_device_graphics[ansible_network_os] }}"
    video:      "{{ __domain_device_video[ansible_network_os] }}"
    input:      "{{ __domain_device_input[ansible_network_os] }}"
    controller: "{{ __domain_device_controller[ansible_network_os] }}"
    memballoon: "{{ __domain_device_memballoon[ansible_network_os] }}"
