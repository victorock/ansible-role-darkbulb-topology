__domain_devices:
  ios:
    console:    "{{ __domain_device_console[ansible_network_os] }}"
    disk:       "{{ __domain_device_disk[ansible_network_os] }}"
    interface:  "{{ __domain_device_interface[ansible_network_os] }}"
    serial:     "{{ __domain_device_serial[ansible_network_os] }}"
  nxos:
    console:    "{{ __domain_device_console[ansible_network_os] }}"
    disk:       "{{ __domain_device_disk[ansible_network_os] }}"
    interface:  "{{ __domain_device_interface[ansible_network_os] }}"
    serial:     "{{ __domain_device_serial[ansible_network_os] }}"
#    graphics:   "{{ __domain_device_graphics[ansible_network_os] }}"
#   controller_usb:
#   controller_sata
#   controller_pci
